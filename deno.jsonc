{
    "tasks": {
        "build": "deno run -Ar scripts/build.ts",
        "test": "deno test -Ar tests/*.ts",
        "check": "deno check **/*.ts",
        "bench": "deno bench -Ar bench/*.ts"
    }
}